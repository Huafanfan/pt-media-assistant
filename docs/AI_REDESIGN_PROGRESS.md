# AI 推荐重设计：实施与恢复入口

更新：2026-09-10。用户已批准按联网检索方案实施，要求持续更新文档以便额度中断后恢复。

## 最高优先级纠正（恢复时必须先读）

- 用户明确指定：只用gpt-5.6-luna、最低推理none；不要Gemini或其他目录别名。
- 正确网关来自本机IVAN_ONLINE_API_URL / IVAN_ONLINE_API_KEY。已只读确认URL为https://api.ivan-online.xyz/v1/chat/completions，key存在，禁止打印值。
- 之前错误使用了TRANS_STATION指向的网关。下面所有旧网关耗时、Gemini目录/别名及低推理协议限制结论都不适用于用户指定服务，仅保留作排查历史，不能据此选型或部署。
- 已完成切换：loadConfig优先选择IVAN整对配置（不跨网关混用key）；新流程独立模型切换选项已撤销，显式评测锁定Luna+none。线上旧镜像通过兼容配置使用同一IVAN地址和密钥，应用healthy、/api/live=200；线上Luna最小请求成功，2615ms。
- 线上仅换网关配置，镜像仍为54c366ee826a9fcc3fbd55150a18ab6b7072af9b0ff6010b5f3e313da275e40b。旧配置备份：/srv/app/pt-media-assistant/backups/gateway-ivan-20260910/env.ai；新密钥文件0600、UID/GID1000，禁止记录密钥值。
- 正确网关首轮完整评测：做饭综艺首卡17.8s（模型15.4s）；轻松喜剧25s（搜索10s、模型15s）；切主题首卡12.1s。发现模型复制整集标题且重复推荐同节目，现已明确作品名归一要求，并缩短理由、省略不需要的引文和偏好字段，正在复测。不能把最小请求2.6s当推荐端到端耗时。
- 最近全量验证：174 tests通过，typecheck/build通过；调整提示词后174 tests及3个评测脚本严格类型检查再次通过。PT评测仍为stub，不证明真实片源。
- 提示词精简复测完成：做饭综艺首卡12.251s、总13.133s，透鲜滴星期天/周六做好菜身份均verified；轻松喜剧首卡13.218s、总13.794s，2部verified、1部unverified。模型仍耗时10.9–12.6s，尚未达成速度目标；单次样本不证明稳定性能。
- 下一步：在Luna none上进一步减少输出协议冗余或验证增量生成首卡，不引入其他模型；补齐代表性质量评测。此前session1055/61631/89435均已结束，不必再轮询。新版尚未部署、提交、推送。

## 当前状态

### 最新发布记录（优先于下方实施历史）

2026-09-10 按用户“部署提交推送”授权发布联网推荐版。镜像 `sha256:e88b9216d8f8755a675ac29c91cd1375d1a0b4dd77deb7a8faf2d42ea2bed0f7`，linux/amd64；启用 `compose.ai-web.yaml`，运行配置已确认 IVAN + gpt-5.6-luna + webEnabled。

- 全量174测试、类型检查、构建通过。服务器容器healthy、unless-stopped，LAN监听192.168.1.2:4178；管理HTTP/HTTPS均200，Prowlarr启动时间未变。
- Mac经真实会话/CSRF和NDJSON接口请求“做饭的综艺”：首卡13.965s、完成19.509s，透鲜滴星期天/周六做好菜均身份verified，有公开来源；PT均unavailable，无错误/警告，未执行下载。推荐首卡速度仍有优化空间。
- 部署源码 `/srv/app/pt-media-assistant/source-ai-web-20260910`；配置/旧镜像备份 `backups/ai-web-20260910`。回滚：`ssh root@192.168.1.2 /srv/app/pt-media-assistant/backups/ai-web-20260910/rollback.sh`。
- 本次提交位于 `codex/ai-discovery-v2`；下方“尚未部署”均为之前阶段的历史记录。

- 基线：`e3fd0c6`，开始时工作区干净；当前独立分支`codex/ai-discovery-v2`，未提交，线上仍运行上一版。
- 本轮目标：保留AI SDK；增加搜索驱动候选发现、主题/偏好语义、带来源推荐、异步PT检查；先完成离线评测与隔离原型，不以一次成功代替质量验收。
- 不把“安装Pi”作为必要条件。不把联网搜索结果当PT资源证明。未核实身份不得下载，下载仍需预检和明确确认。
- 当前阶段：兼容契约、新搜索驱动服务、NDJSON路由与配置已写入；搜索适配器/渐进UI由代理实现中。尚未集成验证，勿部署。
- 本轮尚未部署、提交、推送；不要把在研原型当线上已完成。
- 额度中断后用户已要求继续。两个代理均因额度耗尽退出，主模型接管验收；不要再等待它们自动完成。

## 已确认问题

1. 泛主题发现依赖模型想片名；没有通用web search工具。
2. includeGenres全量AND硬过滤，会误杀“做饭”等元数据没有对应标签的主题。
3. 旧偏好可能影响新主题；需要区分显式偏好、本轮条件、推断条件。
4. 推荐等待PT全部完成；任何来源异常容易被表现为“没有推荐”。
5. 当前Luna Chat Completions工具模式明确只支持reasoning_effort=none；Responses及备选模型能力尚未实测，不能默认可用。

## 接续时先做

1. 读取本文、`git status --short`、`git diff --stat`，确认未完成改动和代理报告；不要重复部署或重跑付费评测。
2. 检查下面的任务状态，优先补齐测试与契约，保留其他代理改动。
3. 每完成一个可验证切片，立即更新本文件：文件范围、命令与结果、未完成项、阻碍。

## 计划与验收

- [ ] 固化搜索与推荐契约：来源证据、未核实候选、内容形态、状态区分。
- [ ] 搜索适配器：Tavily固定服务端端点、超时、结果校验、公共查询缓存、取消、无正文/密钥日志；提供可注入测试接口。
- [ ] 30条评测需求（至少10条综艺/主题），包含切换主题、别名、季、故障与明确排除。
- [ ] 搜索驱动推荐原型：先检索证据，再选择/核实候选；软主题评分与硬条件分离。
- [ ] 首批推荐先展示，PT按卡异步更新；取消/过期请求不得覆盖新状态。
- [ ] 前端来源卡与待核实状态，未核实不进入下载；保留现有确认边界。
- [ ] 类型检查、定向及全量测试、生产构建、桌面/手机交互验证。
- [ ] 经能力与凭据检查后少量真实隔离评测，记录分阶段耗时/用量；不把目标p50≤5秒、p95≤12秒写成已达成。
- [ ] 满足质量、速度与费用要求后再准备部署；明确记录部署授权与验证。

## 当前阻碍/待核实

- Tavily本机CLI此前查询30秒未返回；搜索API密钥是否可用、服务器可达性与中文召回尚未验证。只检查凭据是否存在，不打印值。
- TMDB补充源凭据和覆盖率未验证，不应成为首版单点依赖。
- 原调研文档在`/tmp/pt-ai-redesign-research-20260910.md`；关键决策已保存在本文，临时文件失效不影响恢复。

## 验证记录

- 既有基线：128项测试、类型和构建通过（上一轮）；本轮新增功能尚无通过记录。
- 恢复后重新验证：161项测试、类型检查、完整生产构建通过。provider.ts的reasoning override已部分落地但专门测试尚未创建；浏览器QA尚未执行，是接续优先项。
- 主模型已补provider-reasoning.test.ts（4项通过），流中跨request ID拒绝并取消未读流。新流程low只通过显式评测开启，默认仍none，不自动切档或重试。
- low对照：variety-cooking返回AI_INVALID_OUTPUT，movie-comedy首卡9.796秒/总10.871秒、3部身份核实。**low未通过稳定性验证，不设默认**。已加错误原因分类供后续定位截断/格式/schema，不打印原文。
- 发现搜索引擎baidu.com/link经参数清理后成为无效来源，现直接过滤此类重定向，新增测试待补。
- Chrome CUA本地fixture已验证：首卡先显示且未核实时详情按钮禁用，随后身份核实/片源状态更新；截图查看过，无布局重叠。手机尺寸、取消和控制台尚待验收。使用的本地端口由中断代理启动：Vite PID45297/5173，fixture backend PID45757/4178，收尾须清理。
- Chrome CUA验收补齐：390×844视口页面宽375≤390，输入框底部844在视口内；取消后已展示卡保留、状态变“已取消本轮推荐”，控制台error/warn为空；临时视口已reset。真实网络/模型质量与fixture UI验收分开。
- 开放详情的后台快照同步回归已补齐：未手动刷新时更新，手动刷新后不被后台旧流覆盖，2项测试通过。
- 最新正在显式复测movie-comedy、variety-cooking、switch-topic，默认none，PT仍为桩；待记录结果后清理本地QA进程。
- 最新none复测：做饭首卡17.823秒（模型15.956秒），喜剧首卡22.485秒（模型21.641秒），相关候选已恢复但速度未达标；switch-topic约37.76秒后MODEL_SCHEMA_INVALID。**仍禁止宣称整体验收完成/上线就绪**。
- 已新增scripts/ai-provider-probe.ts。网关目录GET通过约394ms，确认gpt-4.1-mini等轻量型号存在（仅目录不代表生成可用）。正显式比较gpt-4.1-mini三个固定场景；线上默认未变。另单次Luna120-token流式探针执行中。
- gpt-4.1-mini对照：做饭首卡12.536秒，喜剧/切换主题同样因preferences:unrecognized_keys拒绝。根因已确定为模型附加描述字段触发严格schema，不是模型不认识节目。已改模型输出白名单读取/忽略附加说明，关键字段类型仍严格校验（字符串false不能变成布尔值），新增测试；默认Luna两个失败场景复测中。
- Luna流式探针确实支持SSE，但该次首文本12.246秒、总13.334秒；不能假设开流就能解决当前模型首响应延迟，因此未冒进重写模型流解析。
- 本地QA进程PID45297、45757已清理；手机视口已重置。尚未部署提交推送。
- 主模型最新168项测试/类型/构建通过；此后只新增探针和错误原因细分，须最终检查scripts独立类型。
- 白名单修复后喜剧已成功返回3部核实作品，但模型耗时仍19.4秒；跨主题又定位preferences.mood为null。现已将非nullable偏好的null视为“未提供补丁”，不清除旧硬条件；数值年份/数字字符串及4K表达做等价规范化，字符串布尔值仍拒绝。资源硬限制由现有parseQuery独立保留，不依赖模型是否输出。新增测试后核心15项通过，switch-topic最终复测中。
- switch-topic最终通过：新一轮首卡16.094秒，两部综艺身份核实；总31.665秒包含前置电影轮。无格式错误，旧年代/类型已重置。当前171项测试、类型、生产构建、3个脚本独立严格类型检查通过。
- 主功能原型已接通，但速度仍不达目标；继续检查目录中实际Flash类候选（只读），如存在再单场景对照。未切换线上配置。
- 网关实际列出的别名gemini-3.5-flash-lite单场景通过：做饭首卡4.559秒，总5.205秒，模型3.240秒，两部综艺身份核实、官方来源、PT为桩。仅证明该网关别名行为，不声称验证了供应商底层型号。正在3场景进一步对照，未设为线上默认。
- Flash Lite后续：喜剧首卡5.135秒/总6.127秒，3部核实；跨主题新轮首卡4.723秒，2部综艺核实；纪录片零结果定位到本地规则把“不要综艺”误判成要求综艺。已修复否定词识别并补回归，正在纪录片/韩国综艺/硬年份3场景复测。
- 已新增PT_MEDIA_AI_WEB_MODEL可选配置，只影响启用的新流程，不填写则继承原模型；compose.ai-web.yaml可传该变量，旧流程仍保持原模型。最终模型选择须依据评测，不默默切换现网。
- 新增web-recommendation.test.ts 7项通过，覆盖首卡不等PT、主题不硬过滤、来源故障、伪造来源、onlyAvailable分区、旧电影条件切换、取消晚返回。当前类型检查通过（2026-09-10 15:14本地）。
- 真实搜索初次在context-mode进程返回401，查明该工具进程没有继承TAVILY_API_KEY；不能认定密钥无效。已新增显式单次探针scripts/ai-search-probe.ts，经正常登录shell执行验证中。
- Tavily正常登录shell+允许网络的单次探针通过：3.442秒，3条有效结果。但多为哔哩哔哩搜索页/豆瓣讨论，官方覆盖不足，不能据此宣布质量达标。沙箱内41ms失败是网络限制，不是凭据故障。
- 已新增30条评测场景scripts/fixtures/ai-discovery-cases.json（前12条综艺/主题），显式评测脚本scripts/ai-discovery-evaluate.ts每次仅允许1–3个case；真实搜索+模型+豆瓣，PT为桩，必须人工核对相关性，不自动宣布90%成功。
- 正在单场景variety-cooking真实隔离评测；尚未记录通过结论。
- 首个variety-cooking：首卡7.256秒，总8.560秒，3个来源卡全部未核实；至少“小森林”来自相关推荐，烹饪相关性证据不足，**质量未通过**。不能作为验收成功。正在加官方域名优先检索、禁止导航/相关推荐作证据，并核对实体匹配。评测脚本Douban参数顺序已修正（Douban为id,type，Discovery为type,id）。
- 官方优先+标题锚定复测：首卡10.312秒，总10.617秒；2个官方来源候选，但“请给一顿饭Show”偏吃饭而非做饭，仍需质量改进。没有宣布达标。
- 已查明上游搜索将中餐厅各季、可以给你做顿饭吗、型男大主厨全部标movie（只读真实查询）。新流程已取消搜索type硬拦截，仍要求精确标题/年份及详细身份一致；系列名不能自动冒充某一季，新增两个测试覆盖。
- 搜索适配器11项测试通过，域名限定已支持并隔离缓存；首查询官方平台域名、第二查询开放来源。源码消息上下文使用短来源ID减少tokens。
- 评测脚本独立严格类型检查通过：`npx tsc --ignoreConfig --noEmit --module NodeNext --moduleResolution NodeNext --target ES2023 --esModuleInterop --skipLibCheck --strict --types node scripts/ai-discovery-evaluate.ts scripts/ai-search-probe.ts`。
- 全量160项测试、类型、构建通过（15:26附近；后续新增原文证据校验测试须复测）。
- 官方来源+类型修复后的variety-cooking：首卡6.564秒、总7.475秒，顶级厨师/周六做好菜/透鲜滴星期天；后两部身份已核实，首部系列/季仍待核实，PT为桩。此样例相关性改善，未达到整体p50或30条质量目标。
- 同批movie-comedy因只允许来源标题含片名而零结果（原文是推荐清单）。已加“连续原文支持”替代单纯标题门槛：清单来源必须引用包含片名的真实连续片段，不能拼接/伪造/来自导航。新增对应测试，真实复测待进行。

## 修改/代理分工

- 主模型：shared/assistant.ts契约、搜索驱动服务、server/app.ts路由/config集成、最终验收、持续更新本文。
- web_search_adapter（Luna）：仅web-search.ts、web-search.test.ts、AI_SEARCH_ADAPTER.md。
- progressive_ui（Luna）：src/client/**、test/client/**、AI_PROGRESSIVE_UI.md。负责流解析、增量展示、取消、未核实卡禁用详情。
- 主模型已新增web-recommendation.ts：2次公共搜索→1次基于证据JSON选择→来源卡先发→最多3候选并行核实/缓存优先PT。当前需补定向测试、身份/硬条件审查、真实隔离验证。
- 已增加conversation-store.ts可选topic/seenSourceTitles，供明确追问继续主题、未核实来源卡“看过了”排除；取消/失败回滚这些状态。新增未知硬年份条件转待核实区，不冒充满足。
- 渐进取消语义已细化：尚未发出快照则失败回滚；已展示快照则保留该公开状态到历史，保证用户取消后“第一部/看过了”的引用与屏幕一致，晚到PT结果不能覆盖。

## 已定接口（2026-09-10）

- 新流程在`PT_MEDIA_AI_WEB_ENABLED=1`时启用，保持旧流程用于回滚；密钥从TAVILY_API_KEY或TAVILY_API_KEY_FILE读取，启动时不打印。
- 首轮最多2次公开搜索→模型根据证据选作品→立即发来源卡→核实身份→PT更新。不把模型请求数作为质量保证。
- 卡片新增可选identityStatus verified/unverified、sources、contentKind；未核实的web ID不能进入媒体详情或下载。
- 响应新增phase verifying/checking/complete、pendingRecommendations（onlyAvailable时待核实列表，不冒充已可用推荐）。
- 新增NDJSON流端点`POST /api/assistant/turns/stream`，事件为`{type:'snapshot',data:AssistantTurnResponse}`或`{type:'error',error,code}`。保留原JSON端点；同一会话/取消权限边界。
- 已检查本地TAVILY_API_KEY与TRANS_STATION_API_KEY存在，TMDB_API_KEY不存在；仅输出布尔值，未试用密钥。
